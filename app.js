const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const form=document.getElementById('discernment-form');const dialog=document.getElementById('interview-dialog');form?.addEventListener('submit',e=>{e.preventDefault();const value=document.getElementById('situation').value.trim();if(!value)return;document.getElementById('situation-recap').textContent='Situation décrite : « '+value+' »';dialog.showModal()});document.querySelector('.dialog-close')?.addEventListener('click',()=>dialog.close());document.querySelector('.dialog-action')?.addEventListener('click',()=>{const facts=document.getElementById('facts').value;localStorage.setItem('institut-v31-facts',facts);dialog.close();alert('Votre réflexion a été enregistrée sur cet appareil.')});
