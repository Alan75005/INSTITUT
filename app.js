const reflection=document.getElementById('reflection');reflection.value=localStorage.getItem('institut-reflection')||'';document.getElementById('save').addEventListener('click',()=>{const value=reflection.value.trim();const msg=document.getElementById('saved');if(!value){msg.textContent='Écrivez d’abord quelques lignes.';return}localStorage.setItem('institut-reflection',value);msg.textContent='Réflexion conservée sur cet appareil.'});document.getElementById('analyse').addEventListener('click',()=>{const value=document.getElementById('situation').value.trim();const result=document.getElementById('result');if(value.length<12){alert('Décrivez la situation en quelques phrases.');return}result.hidden=false;result.scrollIntoView({behavior:'smooth'})});
